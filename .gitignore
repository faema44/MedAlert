# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Chave de conta de serviço do Firebase (FCM). É SEGREDO: quem a tem manda push em nome do app.
# Fica aqui porque o prompt do `eas credentials` só aceita caminho relativo ao projeto, então o
# arquivo acaba passando pela raiz — e um `git add .` distraído o publicaria para sempre.
# (Não confundir com google-services.json, que NÃO é segredo e vai dentro do próprio APK.)
*firebase-adminsdk*.json
fcm.json

# Native
.kotlin/
*.orig.*
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# built APKs (generated by build-apk.bat, large binaries)
/releases

# documentation / design files (not app assets)
assets/s_*.png
assets/screen*.png
assets/ui.xml
bulas/
site/
*.txt

# arquivos de trabalho locais — o repo é público (dbSync via jsDelivr), não publicar
scripts/
assets/screenshots/
*.bat
*.csv
verify_brands.py
store-assets/
PROJETO.md
analise-bulas-med-br/
scripts/cache_rxnav_en.json
scripts/cache_fda_labels.json
src/data/interactions.json.bak-*
