<?xml version="1.0" encoding="utf-8"?>
<!-- minHeight 40dp = 1 célula: o widget pequeno tem de caber onde há espaço de um ícone.
     resizeMode vertical porque é a ALTURA que decide quantas linhas cabem (ver o provider);
     a largura o launcher dá de sobra.
     updatePeriodMillis=0 de propósito: o mínimo do Android é 30 min, e para "próxima dose"
     isso mostraria dado velho. Quem atualiza é o app, a cada reagendamento. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="40dp"
    android:targetCellWidth="3"
    android:targetCellHeight="1"
    android:resizeMode="vertical|horizontal"
    android:widgetCategory="home_screen"
    android:initialLayout="@layout/med_widget_small"
    android:updatePeriodMillis="0"
    android:previewLayout="@layout/med_widget_medium" />
